# Resolve自動配置ツール 仕様書

## 目的
動画フォルダ内の「台本・ナレーション音声・素材」から、台本のシーン順に素材を配置したタイムラインを
DaVinci Resolve Studio上に自動生成する。人間は最終確認だけを行う。テロップは本ツールの対象外。

## 環境
- Windows / NVIDIA GPU あり
- DaVinci Resolve Studio 21.0.3（Resolveの自動アップデートはしない）
- Python 3.13 64bit（python.org版）。当初は3.11の予定だったが、Resolve 21.0.3 の fusionscript.dll は 3.13 向けで、3.11 では読み込めない（アクセス違反／初期化失敗）ことを実機で確認したため変更
- ffmpeg
- 文字起こし・アライメント：stable-ts（Whisper large-v3、CUDA）

## 使い方（完成形）
```
python autoedit.py "D:\動画\動画_AI副業の始め方"
```
Resolveを起動し、プロジェクトを開いた状態で実行する。

## 入力フォルダ構成
```
動画_xxx/
  script.md
  audio/narration.wav   （mp3/m4aも可。audio内の音声ファイルは1本のみ）
  media/
    S01_opening.mp4
    S02_graph.png
    S03a_screen.mp4
    S03b_result.png
  output/               （ツールが生成）
```

## 素材ファイル命名ルール
- `S{2桁番号}_{自由記述}.{拡張子}` … そのシーンの素材が1つ
- `S{2桁番号}{a,b,c...}_{自由記述}.{拡張子}` … 1シーンに複数素材
- 動画：mp4, mov / 静止画：png, jpg, jpeg
- 同じシーン番号で「サフィックスなし」と「a,b…付き」が混在したらエラー

## 台本書式（script.md）
```
## S01
こんにちは、ヒロキです。今日はAI副業の始め方を話します。

## S02
まず結論から言うと、最初の3ヶ月は収益ゼロを覚悟してください。

## S03
[a] 実際の画面を見てください。ここで設定を開きます。
[b] すると、このように結果が表示されます。
```
- `## Sxx` がシーン見出し。見出し以下の本文がナレーション原稿
- 素材が複数あるシーンでは、行頭の `[a]` `[b]` … が素材の切り替え位置（その文から次のマーカーまでが対象）
- `#` で始まる見出し以外の行、空行以外はすべて読み上げ原稿として扱う
- `//` で始まる行はコメント（アライメントに使わない）

## 処理の流れ
1. **台本解析**：シーン・サブセクション（a,b…）ごとに原稿テキストを分割
2. **素材照合**：各セクションに対応する素材ファイルを特定
3. **強制アライメント**：stable-ts の `model.align(音声, 台本全文, language="ja")` で単語単位の時刻を取得し、各セクションの開始・終了時刻を算出
   - セクション開始＝そのセクション最初の単語の開始時刻（最初のセクションは0秒）
   - セクション終了＝次のセクションの開始時刻（最後のセクションは音声の終端）
   - 無音部分は直前のセクションに含める（映像に隙間を作らない）
   - 30fpsのフレーム数に丸める。丸め誤差で隙間・重なりが出ないよう、終了フレーム＝次の開始フレームとする
4. **配置表生成**：`output/plan.json`
5. **尺調整**
   - 動画がセクションより長い：素材の先頭から使い、セクション尺で切る
   - 動画がセクションより短い：動画の直後に「最終フレームの静止画」を置いて埋める。静止画は ffmpeg で最終フレームを `media/_freeze/` にPNG出力して使う
   - 静止画：セクション尺そのまま
6. **Resolveへ配置**（Python Scripting API）
   - 現在開いているプロジェクトに新規タイムラインを作成。名前は `{フォルダ名}_{YYYYMMDD_HHMM}`。既存タイムラインは変更しない
   - タイムライン設定：1080×1920、30fps
   - 解像度が違う素材は「縦を基準に収める（fit）」をデフォルトとし、設定で fill（切り抜き）に切替可能にする
   - メディアプールにフォルダ名のビンを作り、素材とナレーションをインポート
   - V1：映像素材（静止画含む）を plan.json どおりに `AppendToTimeline`（recordFrame・trackIndex指定）で配置
   - A1：ナレーションを0フレームから全長で配置
   - 動画素材に付随する音声は配置しない（映像のみ）
   - 各シーン先頭にマーカーを付ける（名前＝シーン番号、メモ＝原稿の冒頭20文字）
7. **レポート出力**：`output/report.md`

## チェック（report.md に出力。エラーがあれば配置前に停止）
エラー（停止）：
- 台本にあるのに素材がないシーン／マーカー
- 素材はあるのに台本にないシーン／マーカー
- 命名ルール違反、音声ファイルが0本または2本以上

警告（配置は実行）：
- アライメント信頼度の低い区間（台本と実際の読み上げが違う可能性）
- 尺が1秒未満のセクション
- 静止フレームで埋めた尺が2秒以上のセクション（素材不足の可能性）

レポートにはシーンごとの表（シーン番号／開始時刻／尺／素材名／警告）を載せる。

## 設定（config.json）
- `fps`: 30 / `width`: 1080 / `height`: 1920
- `sizing`: "fit" または "fill"
- `whisper_model`: "large-v3"

## 実装の進め方（Claude Codeへ）
各フェーズを完了・動作確認してから次へ進むこと。
- フェーズ0：環境構築（Python 3.13、ffmpeg、CUDA版PyTorch、stable-ts）と、Resolve APIへの接続テスト（プロジェクト名を取得して表示）
  - Resolve側：環境設定 → システム → 一般 → 「外部スクリプトに使用」を「ローカル」
  - 環境変数：`RESOLVE_SCRIPT_API`、`RESOLVE_SCRIPT_LIB`、`PYTHONPATH` を Resolve の Developer\Scripting の README に従って設定
- フェーズ1：台本解析・素材照合・エラーチェック（Resolve・音声処理なしで単体テスト可能にする）
- フェーズ2：強制アライメントと plan.json 生成
- フェーズ3：尺調整（静止フレーム生成）
- フェーズ4：Resolveへの配置
- フェーズ5：テスト素材（1分・3〜4シーン、複数素材シーン1つを含む）で通し実行

## 対象外（将来拡張）
- テロップ、BGM、効果音、トランジション、エフェクト

## 進捗
- フェーズ0：**完了**（`setup.ps1`、`tools/check_env.py`、`tools/resolve_test.py`、`resolve_connect.py`）。実機で確認済み：Python 3.13.5 / ffmpeg 9.0.2 / torch 2.14.0+cu126 で GPU 計算OK / stable-ts 2.19.1 / Whisper large-v3 を GPU に読み込みOK / Resolve Studio 21.0.3.7 に接続しプロジェクト名取得OK
  - 仮想環境は C:\AutoDavinch\venv（G: が NTFS でなく PyTorch を入れられないため）。実行は `.\run.bat <スクリプト>`
  - PC には Python 3.13 と 3.11 が入っている。`py` の既定は 3.13
  - テスト用プロジェクト「Short-Auto0923」のフレームレートは 24。フェーズ4では 30fps のタイムラインを確実に作れるか（プロジェクト設定の timelineFrameRate を含めて）確認すること
- ユーザーのGPUは GeForce GTX 1070（8GB、sm_61）。PyTorch は cu126 版が必要（cu128 版は sm_61 非対応）。fp16 が遅い世代なので、フェーズ2で Whisper の fp16/fp32 と速度・VRAM を確認すること
- フェーズ1：実装済み（`autoedit.py`、`core/script.py` 台本解析、`core/media.py` 素材・音声の読み取り、`core/checks.py` 照合とチェック、`core/report.py` report.md 出力、`tests/` 単体テスト41件、`samples/動画_サンプル`）。**完了**：Windows 実機で単体テスト41件OK、サンプルフォルダでチェックOK（3シーン/4セクション）、存在しないフォルダ指定でエラー表示を確認
  - 仕様に明記がなく決めたこと：最初のシーン見出しより前の原稿・シーン番号の重複・原稿が空のシーン/マーカー・マーカーが [a] から連番でない・マーカー付きシーンで最初のマーカーより前に原稿がある → エラー。シーン番号が昇順でない → 警告（台本の順に並べる）。`# タイトル` など `## Sxx` 以外の `#` 行は無視。`## S1` `# S01` など見出しのつもりで書式が違うものはエラー
  - 素材：拡張子の大文字小文字は区別しない。media/ のサブフォルダ（_freeze など）と Thumbs.db・desktop.ini・ドットファイルは無視。同じシーン/マーカーに素材が2つ以上 → エラー
  - script.md は UTF-8（BOMあり/なし）。Shift_JIS なら読んだうえで警告
  - 台本か素材の読み取りでエラーがあれば、照合（素材の過不足チェック）は行わない
- フェーズ2：実装済み・**実音声での確認待ち**（`core/align.py` アライメントとセクション時刻、`core/plan.py` フレーム化と plan.json、`core/ffmpeg.py` ffprobe、`core/config.py`、単体テスト追加で計66件）。ユーザーの希望で音声なしで先に作成。開発環境からは Whisper モデルも音声合成も取得できず、stable-ts の実行は未確認。Windows 実機で単体テスト66件OK
  - 単語→セクションの対応付け：台本（空白除去・小文字化）と単語列の文字を difflib で対応付け、セクション先頭文字を含む単語の start を開始時刻にする。多少文字が違っても追える。一致度90%未満は警告
  - 信頼度：セクション内の単語の probability の平均。config.json の `low_confidence`（既定0.5）未満で警告。閾値は実音声で要調整
  - stable-ts 2.19.1 の align は fp16 オプションを持たず、モデルの精度（fp32）で計算する。GTX 1070 でも fp16 の遅さは問題にならない
  - 最後のセクションの終了フレーム＝音声長×fps の切り上げ。1本目の開始は常に0フレーム
- フェーズ3：実装済み（`core/fit.py`、`core/ffmpeg.py` に probe_video・extract_last_frame を追加、単体テスト計78件）。開発環境で ffmpeg を使い、テスト動画での最終フレーム書き出し・尺調整・autoedit の通し（Whisper だけ偽物に差し替え）まで確認済み。**完了**：Windows 実機で単体テスト78件OK
  - plan.json の各セクションに `clips`（type: video/freeze/image、record_frame、frames。video は source_in_sec / source_out_sec）と `freeze_frames` を追加。video のセクションは media に duration_sec・fps・width・height も入る
  - 動画の使えるフレーム数＝動画の長さ×タイムラインfps の切り捨て
  - 最終フレームの PNG は毎回書き出す（コピーで差し替えた動画は更新日時が古いままのことがあるため、日時での判定はしない）。ファイル名は `<動画名>_last_<内容のSHA1先頭8桁>.png`。内容が変われば別名にする（Resolve は同じパスの取り込み済み画像を使い続けるうえ、メディアプールから消すと既存タイムラインからも消えるため）。足りている動画の分は作らない
  - アライメント（フェーズ2）でエラーがあれば尺調整は行わない
- フェーズ4：実装済み・**Resolve 実機での確認待ち**（`core/resolve_place.py`、`autoedit.py` に `--no-resolve` `--from-plan` `--fake-align`、`tools/make_sample.py` テスト素材の生成、単体テスト計92件。Resolve の API をまねた偽物でのテストのみ）
  - 実機で確かめること：タイムライン単位の SetSetting で30fps・1080×1920・timelineInputResMismatchBehavior が効くか／AppendToTimeline の endFrame が含む・含まないどちらか（最初のクリップの長さで自動判定する作り）／静止画を5秒（Resolve の既定）より長く置けるか／AddMarker の frameId がタイムライン先頭からの相対か（読み戻して違えば警告）／モノラルのナレーションを A1 に置けるか（置けなければモノラルトラックを足して警告）
  - タイムライン単位で30fpsにできないとき：プロジェクトにほかのタイムラインがなければプロジェクトのフレームレートを変えて作り直す。あればエラー（既存タイムラインは変えない）
  - 素材は1ファイルずつ ImportMedia（まとめると連番画像がシーケンスにされるため）。ビン内に同じファイルパスのクリップがあれば使い回す
  - 置いたクリップの GetStart・GetDuration を plan と照合し、1フレームを超えてずれたらエラーで止める
  - `--fake-align`：Whisper を使わず原稿の文字数比で時間を割り振る。plan.json に `fake_align: true`。録音なしで配置を試すため
  - 実機1回目（Short-Auto0923、プロジェクト24fps、--fake-align のテスト素材）：ビン作成・取り込み・30fps のタイムライン作成・S01（動画）の配置はOK。S02（静止画）が予定135フレームに対し150フレームで置かれて停止。静止画は取り込み時のプロジェクトfps（24）と既定の長さ（5秒）を持ち、30fps 換算の endFrame を24fps として読まれ、5秒に切られたと判断
  - 対策：静止画も素材の FPS で endFrame を換算。今回取り込んだ静止画は SetClipProperty("FPS") でタイムラインの fps に合わせる（取り込み済みのものは既存タイムラインへの影響を避けて変えない）。5秒を超える静止画は、置けた長さを読み戻して同じ画像を続けて並べる（動画は頭に戻るので繰り返さない）。静止画の FPS・長さを配置時に表示
