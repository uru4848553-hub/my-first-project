# Resolve 自動配置ツール 取扱説明書

台本・ナレーション・素材を1つのフォルダに入れて実行すると、DaVinci Resolve に
**台本のシーン順に素材を並べ、ナレーションを敷いたタイムライン**を自動で作ります。
あとは人が確認・仕上げをします。

## 1. このツールでできること・できないこと

| できる | できない（手作業で行う） |
|---|---|
| 台本のシーン順に、動画・画像を V1 に並べる | テロップ（字幕）の作成 |
| 読み上げに合わせて、シーンの切り替え位置を自動で決める（Whisper） | BGM・効果音の配置 |
| ナレーションを A1 に0秒から置く | トランジション・エフェクト |
| 動画が足りないときは、最後の画面を静止させて埋める | カラー補正・音量調整 |
| 縦長（1080×1920・30fps）のタイムラインを作り、横長素材は収まるよう縮小（fit） | |
| シーンの頭にマーカー（シーン番号と台本の冒頭）を付ける | |
| 問題があればレポート（report.md）にまとめ、配置前に止める | |

テロップ・BGM・効果音は、仕様書（CLAUDE.md）で「将来の拡張」としていて、今は入っていません。
マーカーのメモに台本の冒頭が入っているので、テロップを入れる位置の目安にできます。

## 2. フォルダの作り方（素材の置き場所）

動画1本につき、フォルダを1つ作ります。フォルダ名は、そのまま Resolve のタイムライン名・ビン名になります。

```
動画_AI副業の始め方\          ← 動画1本分のフォルダ（名前は自由）
  script.md                   ← ① 台本
  audio\                      ← ② ナレーション（1本だけ）
    narration.mp3
  media\                      ← ③ 動画・画像素材
    S01_opening.mp4
    S02_graph.png
    S03a_screen.mp4
    S03b_result.png
  output\                     ← ツールが作る（レポート・配置表）
```

### ① 台本 → フォルダ直下の `script.md`

メモ帳などで作り、**UTF-8** で保存します。

```
## S01
こんにちは、ヒロキです。今日はAI副業の始め方を話します。

## S02
まず結論から言うと、最初の3ヶ月は収益ゼロを覚悟してください。

## S03
[a] 実際の画面を見てください。ここで設定を開きます。
[b] すると、このように結果が表示されます。
```

- `## S01` `## S02` …（S＋2桁の数字）がシーンの区切りです。
- 見出しの下の文章が読み上げ原稿です。**ナレーションと同じ文章**にしてください（違うと切り替え位置がずれます）。
- 1つのシーンで素材を切り替えたいときは、行頭に `[a]` `[b]` … を付けます。その行から次の `[ ]` までが、その素材の範囲です。
- `//` で始まる行はメモ（読み上げない）、`# タイトル` のような見出しも無視されます。

### ② ナレーション → `audio\` フォルダ

- wav / mp3 / m4a のどれか **1本だけ** 入れます。ファイル名は自由です（ElevenLabs のファイル名のままで大丈夫）。
- 台本どおりに読んだ音声にしてください。
- ※ BGM・効果音はここに入れないでください（2本以上あるとエラーになります。BGM には対応していません）。

### ③ 動画・画像素材 → `media\` フォルダ

- 動画：mp4 / mov、画像：png / jpg / jpeg
- **ファイル名の頭を、台本のシーン番号に合わせます。**
  - シーンに素材が1つ：`S01_好きな名前.mp4`
  - シーンに素材が複数（台本に `[a]` `[b]` があるシーン）：`S03a_好きな名前.mp4`、`S03b_好きな名前.png`
- 1つのシーン（または [a] [b]）に素材は1つだけです。
- 動画は頭から使います。シーンより長ければ途中で切り、短ければ最後の画面を止めて埋めます。
- 動画に付いている音声は使いません（映像だけ）。
- `media\_stills\`・`media\_freeze\` はツールが作るフォルダです。触らなくて大丈夫です。

## 3. 実行のしかた（トリガー）

**事前に：DaVinci Resolve を起動し、タイムラインを作りたいプロジェクトを開いておきます。**

### 方法A：ドラッグ＆ドロップ（おすすめ）

`G:\@Auto_Davinch-Cloude\自動編集.bat` に、**動画フォルダをドラッグ＆ドロップ**します。
黒い画面が開いて処理が進み、終わると「Resolve にタイムライン「…」を作成しました」と出ます。何かキーを押すと閉じます。

（`自動編集.bat` をダブルクリックした場合は、フォルダのパスを聞かれるので、貼り付けて Enter）

### 方法B：PowerShell から

```powershell
cd "G:\@Auto_Davinch-Cloude"
.\run.bat autoedit.py "G:\@ゼロからAI副業チャンネル\動画_AI副業の始め方"
```

### 処理の流れと時間のめやす

1. チェック（数秒）：台本・素材・ナレーションがそろっているか
2. アライメント（音声1分あたり約2分）：読み上げとシーンの対応をとる
3. 尺調整（数秒）
4. Resolve に配置（数十秒）：新しいタイムライン `フォルダ名_日付_時刻` を作る。**既存のタイムラインは変更しません**

## 4. 実行したあとにすること

1. Resolve で新しいタイムラインを再生し、切り替え位置・素材を確認します。
2. フォルダ内の `output\report.md` を開き、警告がないか確認します。
   - 「アライメント信頼度が低い」→ 台本と読み上げが違う可能性。台本を直して再実行
   - 「尺が1秒未満」→ シーンが短すぎる。台本の区切りを見直す
   - 「静止フレームで埋めた尺が2秒以上」→ 動画素材が短い。長い素材に差し替えると自然になります
3. テロップ・BGM・効果音などの仕上げは、Resolve で手作業で行います。

## 5. エラーが出たとき

エラーがあると、Resolve に置く前に止まり、画面と `output\report.md` に理由が出ます。よくあるもの：

| 表示 | 直し方 |
|---|---|
| 台本の S02 に対応する素材がありません | `media\` に `S02_〜` のファイルを入れる |
| 素材 S05_〜 のシーン S05 が台本にありません | 台本に `## S05` を足すか、素材を外す |
| 命名ルール違反 | ファイル名を `S01_名前.mp4` の形にする |
| audio フォルダの音声ファイルは1本だけにしてください | 音声を1本にする（BGM などは外す） |
| Resolve に接続できません | Resolve が起動しているか、環境設定 → システム → 一般 →「外部スクリプトに使用」が「ローカル」か確認 |

直したら、同じ操作でもう一度実行します（前に作ったタイムラインはそのまま残り、新しいタイムラインが増えます）。

## 6. 便利なオプション（PowerShell で実行するとき）

| オプション | 内容 |
|---|---|
| `--check-only` | チェックだけ（数秒）。素材をそろえている途中の確認に |
| `--from-plan` | 前回の結果を使って Resolve への配置だけやり直す（アライメントを待たない） |
| `--no-resolve` | Resolve に置かず、レポートと配置表だけ作る |

例：`.\run.bat autoedit.py "G:\...\動画_AI副業の始め方" --check-only`

## 7. 設定（`config.json`）

| 項目 | 内容 |
|---|---|
| `sizing` | 解像度が違う素材の扱い。`"fit"`（全体を収める・余白あり）／`"fill"`（画面いっぱい・はみ出しは切る） |
| `fps` / `width` / `height` | タイムラインのフレームレートと解像度（既定 30 / 1080 / 1920） |
| `low_confidence` | 「信頼度が低い」警告を出す基準（既定 0.5） |

## 8. ツールの更新

```powershell
cd "G:\@Auto_Davinch-Cloude"
git pull origin claude/claude-md-phase-0-qxn5wu
```
