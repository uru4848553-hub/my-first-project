# Resolve 自動配置ツール 取扱説明書

台本・ナレーション・素材を1つのフォルダに入れて実行すると、DaVinci Resolve に
**台本のシーン順に素材を並べ、ナレーションを敷いたタイムライン**を自動で作ります。
あとは人が確認・仕上げをします。

## 0. いちばん簡単な使い方（アプリ）

**最初に1回だけ**：`G:\@Auto_Davinch-Cloude\ショートカット作成.bat` をダブルクリックすると、デスクトップにアイコン付きの「Resolve自動配置」ができます（タスクバーに置きたいときは、右クリック →「タスクバーにピン留めする」）。

以後は、デスクトップの「Resolve自動配置」（または `アプリ起動.bat`）をダブルクリックすると、アプリの画面が開きます。上から順に入れて「▶ スタート」を押すだけです。

| 画面の欄 | 入れるもの |
|---|---|
| ① 動画の名前と保存先 | 動画の名前（タイムライン名になる）と、作業用フォルダを作る場所 |
| ② 台本 | 「絵コンテ画像から作る」で絵コンテの PNG を選ぶ（AI が台本に変換）か、台本を貼り付けるか「ファイルから読み込む」。書き方は下の「2. ① 台本」と同じ |
| ③ ナレーションと BGM | ナレーション（必須、1本）と BGM（なくてもよい、1本）の音声ファイル |
| ④ シーンごとの素材と効果音 | 台本からシーンの一覧（台本に効果音があればその行も）が自動でできる。各行に動画・画像・効果音を選ぶ（行をダブルクリック）。「まとめて追加」で複数選ぶと、ファイル名が `M01`・`M03_1`・`K01`・`S01_` などで始まるものはその行へ、それ以外は上から順に入る |
| ⑤ DaVinci Resolve に並べる | Resolve のプロジェクト名（なければ作る。空にすると今開いているプロジェクト）。「起動していなければ起動する」にチェック |

- 素材のファイル名は自由です（アプリが `S01_元の名前` の形でコピーします）。
- 「スタート」を押すと、保存先に動画フォルダを作って素材をコピー → Resolve を起動（起動していなければ）→ 読み上げとシーンの対応取り → Resolve に並べる、まで自動で進みます。音声1分あたり約2分かかります。
- 終わったら「レポートを開く」で警告を確認できます。入力した内容（台本・保存先など）は次に開いたときも残っています。
- BGM は A2 トラックに、0秒からナレーションの終わりまで置きます（短ければそこで終わり、繰り返しはしません）。**音量は自動では下げないので、Resolve で調整してください。**
- テロップ・効果音は、台本に「テロップ：〜」「効果音：K01」と書くと自動で置きます（下の「テロップと効果音」）。

### 初めてテロップを使う前に（1回だけ）

テロップの文字を画像にする部品（Pillow）を入れます。PowerShell で：

```powershell
cd "G:\@Auto_Davinch-Cloude"
.\run.bat -m pip install -r requirements.txt
```

## 絵コンテ画像（PNG）から台本を作る

② 台本 の「**絵コンテ画像から作る…**」を押して、絵コンテの PNG（jpg・webp も可）を選ぶと、AI（Claude）が画像を読んで台本の文字に変換し、② に入れます。30秒〜1分かかります。

- 複数ページの絵コンテは、Ctrl を押しながら全部選びます（ファイル名の順に読みます）。
- 画像の文字が崩れている所は AI が直し、「直した・推測した箇所」を一覧で表示します。**ナレーションの文章は必ず確認してください**（ナレーション音声は、この台本の文章で作ります）。
- **初めて使うときは API キーが必要です**（1回 数円〜十数円程度）。
  1. https://console.anthropic.com/ にログイン（アカウントがなければ作成）
  2. 「Billing」でクレジットを購入（最低 5 ドル程度）
  3. 「API Keys」→「Create Key」で作成し、`sk-ant-` で始まるキーをコピー
  4. アプリで「絵コンテ画像から作る…」を押したときに出る画面に貼り付け（`app_settings.json` に保存され、次からは聞かれません）
- 部品が入っていない場合は、PowerShell で `.\run.bat -m pip install -r requirements.txt` を実行してください。

図入りの説明は `取扱説明書.pptx`（PowerPoint）にあります。

## テロップと効果音

台本の中に、読み上げない指示の行として書きます。

```
## S01
テロップ：ついに、想像を超えた
テロップ：iPhoneが来る。
効果音：K01
ついに、私たちの想像を超えたiPhoneがやって来ます。

## S03
テロップ：シーン全体に出すテロップ
[1] 最初の部分のナレーション。
効果音：K02 +1.5
[2] 次の部分のナレーション。
テロップ：[2] の間だけ出すテロップ
```

| 書き方 | 動き |
|---|---|
| `テロップ：文字` | その場所の間ずっと、画面にテロップを出す。**1行が画面の1行**（長すぎると自動で折り返すので、区切りたい所で行を分けるのがおすすめ） |
| `効果音：K01` | その場所の頭で、効果音 K01 を鳴らす |
| `効果音：K01 +1.5` | 頭から1.5秒後に鳴らす |

- **場所**：シーン見出し（`## S01`）の直後、`[1]`（`[a]`）より前に書くとシーン全体。`[1]` などの後に書くと、その部分だけ。
- **テロップ**は V2 トラックに置きます（重なるときは V3）。白い文字に黒い縁取り、画面の上のほう。文字の大きさ・位置・色は `config.json` で変えられます（下の「7. 設定」）。
  - Resolve 上では文字を打ち直せないので、直すときは台本を直してもう一度「スタート」してください。
  - テロップの周りが黒くなるときは、メディアプールでテロップのクリップを右クリック → クリップ属性 → アルファモードを「ストレート」にしてください。
- **効果音**は BGM の次のトラック（BGM ありなら A3、なしなら A2。重なるときはさらに次）に、ファイルの長さのまま置きます。音量は Resolve で調整してください。
  - アプリでは、台本に書いた効果音（K01 など）が ④ の表に出てくるので、ファイルを選びます。ファイル名が `K01` で始まっていれば「まとめて追加」で自動で入ります。

## 絵コンテからの台本の作り方

| 絵コンテの項目 | 台本での書き方 |
|---|---|
| カット番号 1, 2, 3 … | `## S01` `## S02` `## S03` … |
| ナレーション | 見出しの下に、そのまま書く（**音声と同じ文章に**） |
| テロップ（S01 …） | `テロップ：文字`（2行なら2行書く） |
| 動画素材（M01、M03_1 …） | 素材の名前。1シーンに複数（M03_1・M03_2）なら、ナレーションを `[1]` `[2]` で分ける |
| 効果音（K01 …） | `効果音：K01` |
| BGM（BGM1） | アプリの BGM 欄で選ぶ |
| カメラ・構図、時間の目安 | `//` を付けてメモとして残す（時間は読み上げから自動で決まる） |

例：`samples\絵コンテ_iPhone\台本.txt`（絵コンテ `絵コンテ_修正版.png` を台本にしたもの）

以下は、アプリを使わずにフォルダを自分で作る場合の説明です。

## 1. このツールでできること・できないこと

| できる | できない（手作業で行う） |
|---|---|
| 台本のシーン順に、動画・画像を V1 に並べる | テロップの細かいデザイン（色分け・動き） |
| 読み上げに合わせて、シーンの切り替え位置を自動で決める（Whisper） | BGM・効果音の音量調整 |
| ナレーションを A1 に0秒から置く | トランジション・エフェクト |
| BGM を A2 に置く（bgm フォルダ・アプリの BGM 欄） | カラー補正 |
| テロップを V2 に置く（台本の「テロップ：」） | |
| 効果音を A3 以降に置く（台本の「効果音：」、se フォルダ） | |
| 動画が足りないときは、最後の画面を静止させて埋める | |
| 縦長（1080×1920・30fps）のタイムラインを作り、横長素材は収まるよう縮小（fit） | |
| シーンの頭にマーカー（シーン番号と台本の冒頭）を付ける | |
| 問題があればレポート（report.md）にまとめ、配置前に止める | |

テロップは白文字＋黒縁の1種類です。一部の文字だけ色を変えるなどの仕上げは Resolve で行ってください。

## 2. フォルダの作り方（素材の置き場所）

動画1本につき、フォルダを1つ作ります。フォルダ名は、そのまま Resolve のタイムライン名・ビン名になります。

```
動画_AI副業の始め方\          ← 動画1本分のフォルダ（名前は自由）
  script.md                   ← ① 台本
  audio\                      ← ② ナレーション（1本だけ）
    narration.mp3
  bgm\                        ← BGM（なくてもよい、1本だけ）
    bgm.mp3
  se\                         ← 効果音（なくてもよい。台本の「効果音：K01」と対応）
    K01_ポン.wav
  media\                      ← ③ 動画・画像素材
    S01_opening.mp4            （M01.mp4 のような絵コンテの名前でもよい）
    S02_graph.png
    S03a_screen.mp4            （M03_1.mp4）
    S03b_result.png            （M03_2.png）
  output\                     ← ツールが作る（レポート・配置表）
```

### ① 台本 → フォルダ直下の `script.md`

メモ帳などで作り、**UTF-8** で保存します。

```
## S01
こんにちは、ヒロキです。今日はAI副業の始め方を話します。

## S02
まず結論から言うと、最初の3ヶ月は収益ゼロを覚悟してください。

## S03
[a] 実際の画面を見てください。ここで設定を開きます。
[b] すると、このように結果が表示されます。
```

- `## S01` `## S02` …（S＋2桁の数字）がシーンの区切りです。
- 見出しの下の文章が読み上げ原稿です。**ナレーションと同じ文章**にしてください（違うと切り替え位置がずれます）。
- 1つのシーンで素材を切り替えたいときは、行頭に `[a]` `[b]` …（または `[1]` `[2]` …）を付けます。その行から次の `[ ]` までが、その素材の範囲です。
- `テロップ：〜` `効果音：K01` の行は読み上げず、テロップ・効果音の指示になります（上の「テロップと効果音」）。
- `//` で始まる行はメモ（読み上げない）、`# タイトル` のような見出しも無視されます。

### ② ナレーション → `audio\` フォルダ

- wav / mp3 / m4a のどれか **1本だけ** 入れます。ファイル名は自由です（ElevenLabs のファイル名のままで大丈夫）。
- 台本どおりに読んだ音声にしてください。
- ※ BGM はここではなく `bgm\` フォルダに入れてください（audio に2本以上あるとエラーになります）。

### ③ 動画・画像素材 → `media\` フォルダ

- 動画：mp4 / mov、画像：png / jpg / jpeg
- **ファイル名の頭を、台本のシーン番号に合わせます。**
  - シーンに素材が1つ：`S01_好きな名前.mp4` または `M01.mp4`（`M01_好きな名前.mp4`）
  - シーンに素材が複数（台本に `[a]` `[b]` があるシーン）：`S03a_好きな名前.mp4`、`S03b_好きな名前.png`、または `M03_1.mp4`、`M03_2.png`
- 1つのシーン（または [a] [b]）に素材は1つだけです。
- 動画は頭から使います。シーンより長ければ途中で切り、短ければ最後の画面を止めて埋めます。
- 動画に付いている音声は使いません（映像だけ）。
- `media\_stills\`・`media\_freeze\` はツールが作るフォルダです。触らなくて大丈夫です。

## 3. 実行のしかた（トリガー）

**事前に：DaVinci Resolve を起動し、タイムラインを作りたいプロジェクトを開いておきます。**

### 方法A：ドラッグ＆ドロップ（おすすめ）

`G:\@Auto_Davinch-Cloude\自動編集.bat` に、**動画フォルダをドラッグ＆ドロップ**します。
黒い画面が開いて処理が進み、終わると「Resolve にタイムライン「…」を作成しました」と出ます。何かキーを押すと閉じます。

（`自動編集.bat` をダブルクリックした場合は、フォルダのパスを聞かれるので、貼り付けて Enter）

### 方法B：PowerShell から

```powershell
cd "G:\@Auto_Davinch-Cloude"
.\run.bat autoedit.py "G:\@ゼロからAI副業チャンネル\動画_AI副業の始め方"
```

### 処理の流れと時間のめやす

1. チェック（数秒）：台本・素材・ナレーションがそろっているか
2. アライメント（音声1分あたり約2分）：読み上げとシーンの対応をとる
3. 尺調整（数秒）
4. Resolve に配置（数十秒）：新しいタイムライン `フォルダ名_日付_時刻` を作る。**既存のタイムラインは変更しません**

## 4. 実行したあとにすること

1. Resolve で新しいタイムラインを再生し、切り替え位置・素材を確認します。
2. フォルダ内の `output\report.md` を開き、警告がないか確認します。
   - 「アライメント信頼度が低い」→ 台本と読み上げが違う可能性。台本を直して再実行
   - 「尺が1秒未満」→ シーンが短すぎる。台本の区切りを見直す
   - 「静止フレームで埋めた尺が2秒以上」→ 動画素材が短い。長い素材に差し替えると自然になります
3. テロップ・効果音・BGM の音量などの仕上げは、Resolve で手作業で行います。

## 5. エラーが出たとき

エラーがあると、Resolve に置く前に止まり、画面と `output\report.md` に理由が出ます。よくあるもの：

| 表示 | 直し方 |
|---|---|
| 台本の S02 に対応する素材がありません | `media\` に `S02_〜` のファイルを入れる |
| 素材 S05_〜 のシーン S05 が台本にありません | 台本に `## S05` を足すか、素材を外す |
| 命名ルール違反 | ファイル名を `S01_名前.mp4` の形にする |
| audio フォルダの音声ファイルは1本だけにしてください | 音声を1本にする（BGM などは外す） |
| 台本の効果音 K02 のファイルがありません | `se\` に `K02_〜.wav` を入れる（アプリなら ④ で K02 の行にファイルを選ぶ） |
| テロップを作るには Pillow が必要です | 上の「初めてテロップを使う前に」の1行を実行する |
| Resolve に接続できません | Resolve が起動しているか、環境設定 → システム → 一般 →「外部スクリプトに使用」が「ローカル」か確認 |

直したら、同じ操作でもう一度実行します（前に作ったタイムラインはそのまま残り、新しいタイムラインが増えます）。

## 6. 便利なオプション（PowerShell で実行するとき）

| オプション | 内容 |
|---|---|
| `--check-only` | チェックだけ（数秒）。素材をそろえている途中の確認に |
| `--from-plan` | 前回の結果を使って Resolve への配置だけやり直す（アライメントを待たない） |
| `--no-resolve` | Resolve に置かず、レポートと配置表だけ作る |

例：`.\run.bat autoedit.py "G:\...\動画_AI副業の始め方" --check-only`

## 7. 設定（`config.json`）

| 項目 | 内容 |
|---|---|
| `sizing` | 解像度が違う素材の扱い。`"fit"`（全体を収める・余白あり）／`"fill"`（画面いっぱい・はみ出しは切る） |
| `fps` / `width` / `height` | タイムラインのフレームレートと解像度（既定 30 / 1080 / 1920） |
| `low_confidence` | 「信頼度が低い」警告を出す基準（既定 0.5） |
| `telop_size` | テロップの文字の大きさ（ピクセル、既定 80） |
| `telop_y` | テロップの縦位置（画面の上端 0 〜 下端 1、既定 0.25＝上から4分の1） |
| `telop_color` / `telop_stroke_color` / `telop_stroke_width` | 文字の色・縁取りの色・縁取りの太さ（既定 白・黒・8） |
| `telop_font` | フォントファイル（空ならメイリオ Bold など。例 `"C:\\Windows\\Fonts\\YuGothB.ttc"`） |
| `telop_max_width` | 1行の最大幅（画面の幅に対する割合、既定 0.9。超えると折り返す） |

## 8. ツールの更新

```powershell
cd "G:\@Auto_Davinch-Cloude"
git pull origin claude/claude-md-phase-0-qxn5wu
```
